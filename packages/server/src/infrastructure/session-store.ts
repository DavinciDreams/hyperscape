/**
 * @deprecated Re-export shim.
 *
 * `session-store` relocated to
 * `packages/shared/src/infrastructure/session-store.ts`
 * as part of the engine/game separation (PLAN_SERVERNETWORK_MIGRATION.md Step 4).
 * Delete after Step 8.
 */

export * from "../../../shared/src/infrastructure/session-store";

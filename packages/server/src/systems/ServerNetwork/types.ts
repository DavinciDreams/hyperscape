/**
 * @deprecated Re-export shim.
 *
 * Relocated to
 * `packages/shared/src/systems/server/network/types.ts` as part of the
 * ServerNetwork → @hyperforge/shared migration
 * (PLAN_SERVERNETWORK_MIGRATION.md Step 1 — zero-dep leaves).
 *
 * Pure payload type definitions. Zero runtime dep. Delete this shim after
 * Step 8.
 */

export * from "../../../../shared/src/systems/server/network/types";

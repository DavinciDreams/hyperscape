/**
 * @deprecated Re-export shim.
 *
 * `TickSystem` relocated to
 * `packages/shared/src/systems/server/TickSystem.ts`
 * as part of the engine/game separation (PLAN_SERVERNETWORK_MIGRATION.md Step 4).
 * Delete after Step 8.
 */

export * from "../../../shared/src/systems/server/TickSystem";

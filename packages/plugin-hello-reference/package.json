{
  "name": "@hyperforge/plugin-hello-reference",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal reference plugin. Proves the @hyperforge/gameplay-framework author surface works end-to-end — a valid plugin.json manifest, a typed PluginFactory<HelloContext>, and integration tests that drive the full lifecycle against shared's PluginHost runtime. Pattern template for I2 (@hyperforge/combat) and all future external plugin packages.",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./manifest": {
      "types": "./dist/manifest.d.ts",
      "import": "./dist/manifest.js"
    }
  },
  "files": [
    "dist/",
    "plugin.json"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@hyperforge/gameplay-framework": "*",
    "@hyperforge/manifest-schema": "*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.0.6"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "GPL-3.0"
}

{
  "name": "@hyperforge/ui-framework",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Widget + layout contract for Hyperforge UI. Widgets are typed, schema-declared building blocks (HP bar, inventory, chat, minimap, etc.) that the UI layout manifest composes into a game's HUD. Rendering is framework-agnostic at the contract level; consumers bind their own ComponentType at registration time.",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.0.6"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "GPL-3.0"
}
